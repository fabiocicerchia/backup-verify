# Copy to .env and fill in. Never commit the real .env.
# backup-verify is config-file driven; env vars are only used by the shell
# commands in your plan's `fetch`/`restore` steps (e.g. cloud credentials).
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# RESTIC_PASSWORD=
